package main

import (
	"fmt"
)

func deployApp(submissionData map[string]interface{}) {
	// gitRepoURL := submissionData["git_repo"].(string)
	// serverIP := submissionData["server_name"].(string)
	// dnsPrefix := submissionData["dns"].(string)
	fmt.Println("Implement this function")
}
